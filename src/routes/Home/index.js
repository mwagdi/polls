import React from "react";

import { QuestionListContainer } from "containers";

export default () => (
  <div className="main">
    <h1 className="main__title">Questions</h1>
    <QuestionListContainer />
  </div>
);
